





<div class="login-wrapper">
  <div class="login-card shadow">
    <div class="avatar-circle">
      <i class="bi bi-person-fill"></i>
    </div>
    <div class="card-body">
      <slot />
    </div>
  </div>
</div>

<style>
  body, html {
  height: 100%;
  margin: 0;
  padding: 0;
}
.login-wrapper {
  width: 100%;
  max-width: 600px; /* Aumenta el ancho aquí */
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1rem;
}

  .login-card {
    width: 100%;
    position: relative;
    padding: 2.5rem 1.5rem 1.5rem;
    border-radius: 12px;
    background: #fff;
    margin-top: 40px; /* Margen positivo para el avatar */
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }
  
  .avatar-circle {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 80px;
    height: 80px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(#4b7cf3, #1e2d4d);
    border: 5px solid #fff;
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.2);
    font-size: 2.2rem;
    color: #fff;
  }

  @media (min-width: 576px) {
    .login-card {
      padding: 4rem 2rem 2rem;
      margin-top: 50px;
    }
    
    .avatar-circle {
      width: 90px;
      height: 90px;
      font-size: 2.8rem;
    }
  }
  .forgot-password {
  text-align: right;
  margin-top: 4px;
}

.forgot-link {
  font-size: 0.875rem;
  color: #4b7cf3;
  text-decoration: none;
}

.forgot-link:hover {
  text-decoration: underline;
}

</style>